name: Test Product Service

on:
  push:
    paths:
      - 'microservices/product_service/**'
  pull_request:
    paths:
      - 'microservices/product_service/**'

jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: microservices/product_service

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: |
          pytest --cov=product_service --cov-report=term-missing -v
